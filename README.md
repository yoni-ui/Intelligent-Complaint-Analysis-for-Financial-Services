# RAG Complaint Chatbot

Repository scaffold for a retrieval-augmented generation chatbot for complaint analysis.

Structure:
- data/: raw and processed datasets
- notebooks/: EDA and preprocessing
- src/: source modules (chunking, embeddings, vector store)
- vector_store/: persisted vectors

