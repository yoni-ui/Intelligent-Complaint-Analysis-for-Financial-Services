# Core dependencies
pandas>=2.0.0
numpy>=1.24.0

# Visualization (optional, for notebooks)
matplotlib>=3.7.0
seaborn>=0.12.0

# Embeddings & Vector Store
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
langchain-text-splitters>=0.0.1

# LLM (Ollama client)
requests>=2.31.0

# UI
gradio>=4.0.0

# Configuration & Environment
python-dotenv>=1.0.0

# Utilities
tqdm>=4.65.0

# Production dependencies
# Logging (built-in, but ensure compatibility)
# Caching (using built-in dict, no extra deps needed)
# Health checks (using built-in modules)

# Optional: For production monitoring (uncomment if needed)
# prometheus-client>=0.18.0
# psutil>=5.9.0
